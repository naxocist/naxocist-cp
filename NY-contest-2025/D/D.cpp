#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n, m; cin >> m >> n;
	for(int i=0; i<n; ++i) {
		string s; cin >> s;
		 for(int j=0; j<m; ++j) {
			 char c = s[j];

		 }
	}
	return 0;
}
