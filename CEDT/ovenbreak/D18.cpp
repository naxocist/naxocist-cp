#include <bits/stdc++.h>
using namespace std;

#define ln '\n'
#define ll long long
#define all(x) begin(x), end(x)
#define pb emplace_back

const int N = 3e3 + 1;
int dp[N][N];

int32_t main() {
  cin.tie(nullptr)->sync_with_stdio(0);
  int l, n; cin >> l >> n;

  vector<pi> v(n);
  for(int i=0; i<n; ++i) {
    int x, c, v; cin >> x >> c >> v;
  }

  return 0;
}